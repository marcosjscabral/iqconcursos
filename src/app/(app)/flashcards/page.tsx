/**
 * Página de listagem de decks (Flashcards).
 * Placeholder da Fase 0 — será implementada na Fase 1.
 */
export default function FlashcardsPage() {
  return (
    <div className="flex flex-col items-center justify-center min-h-[60vh] text-center gap-4">
      <span className="material-symbols-outlined text-6xl text-primary">layers</span>
      <h1 className="font-headline-md text-headline-md text-on-surface">Flashcards</h1>
      <p className="font-body-md text-body-md text-on-surface-variant max-w-sm">
        Seus decks de flashcards vão aparecer aqui. Em construção na Fase 1.
      </p>
    </div>
  );
}
